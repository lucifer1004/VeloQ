#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-07-30-009"
title = "Batch NSys graph replay enrichment"
status = "done"
created = "2026-07-30"
started = "2026-07-30"
completed = "2026-07-30"
refs = [
    "RFC-0006:C-CUDA-IDENTITY",
    "RFC-0006:C-ROW-KEYS",
]

[content]
description = "Replace per-replay launcher and node-event SQL with bounded set-based enrichment so graph-replays scales with selected evidence rather than issuing repeated whole-table queries, while preserving process-aware replay identity and the existing response contract."

[[content.acceptance_criteria]]
text = "Graph replay launcher and node enrichment uses a bounded number of SQL statements independent of the requested replay limit."
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "Node-mode busy time, idle time, top-node decomposition, launcher row ids, ordering, totals, and process-aware replay identities remain equivalent to the established per-replay behavior."
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "Focused tests cover multiple selected replays with overlapping process-local CUDA ordinals and real-profile measurements confirm that larger limits no longer scale through repeated SQL scans."
status = "done"
category = "chore"

[verification]
required_guards = ["GUARD-NSYS-WIRE-CONTRACT"]
