#:schema ../schema/guard.schema.json

[govctl]
id = "GUARD-SOURCE-REGISTRY-CONTRACT"
title = "source registry contract"
refs = ["RFC-0004"]

[check]
command = "cargo test --profile ci -p veloq --bin veloq"
timeout_secs = 300
